services:
  rpgc-bot:
    container_name: rpgc-bot
    image: rpgc-bot
    build:
      context: .
      args:
        ugid: ${UGID}
    env_file: ./.env
    volumes:
      - ./data:/home/rpgc/data
    networks:
      - rpgc-network
networks:
  rpgc-network:
    name: rpgc-network