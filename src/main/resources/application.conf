bot {
  token = ${?TELEGRAM_BOT_TOKEN}

  userWhitelist = ${?USER_ID_WHITELIST}
  gameList = ${?GAME_NAME_LIST}
}

telegraph {
  username = rpgc_bot
  accessToken = ${?TELEGRAPH_ACCESS_TOKEN}
}

database {
  url = "jdbc:h2:"${?DATABASE_DIR_PATH}"/rpgc_bot;FILE_LOCK=FS"

  driver = org.h2.Driver

  username = rpgc_bot
  password = ${?DATABASE_PASSWORD}
}